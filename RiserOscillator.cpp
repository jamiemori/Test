/*
  ==============================================================================

    RiserOscillator.cpp
    Created: 18 Jan 2015 3:22:46pm
    Author:  Yoda

  ==============================================================================
*/
/*
#include "RiserOscillator.h"

RiserOscillator::Oscillator()
{
	if waveform ()

	const float currentSample = (float) sin(currentAngle);

                        for (int i = outputBuffer.getNumChannels(); --i >= 0;)
                            outputBuffer.addSample (i, startSample, currentSample);

                        currentAngle += angleDelta;
                        ++startSample;
                        tailOff *= 0.99;

                        if (tailOff <= 0.005)
                        {
                            clearCurrentNote();
                            angleDelta = 0.0;
                        }
 const float currentSample = (float) sin(currentAngle);

                        for (int i = outputBuffer.getNumChannels(); --i >= 0;)
                            outputBuffer.addSample (i, startSample, currentSample);

                        currentAngle += angleDelta;
                        ++startSample;

}

*/